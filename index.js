var fs = require('fs')

var dest_source = '.source'

// var source = require('./tools/source')
// source('-b source -l -s .', dest_source).then(function(Repo) {
// }).catch(function(err) {
// 	console.error(err.stack || err)
// })

